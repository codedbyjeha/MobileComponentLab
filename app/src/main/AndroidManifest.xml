<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mobilecomponetlab.GisellaPutri">

    <!-- ⚠️ Permissions -->
    <uses-permission android:name="android.permission.SET_ALARM" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MobileComponetLab">

        <!-- 🏠 Activity Utama -->
        <activity
            android:name="com.mobilecomponetlab.GisellaPutri.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ⏰ Alarm -->
        <activity android:name="com.mobilecomponetlab.GisellaPutri.alarm.AlarmActivity" />
        <receiver android:name="com.mobilecomponetlab.GisellaPutri.alarm.AlarmReceiver" />

        <!-- 📋 RecyclerView -->
        <activity android:name="com.mobilecomponetlab.GisellaPutri.recyclerview.RecyclerViewActivity" />

        <!-- 🗃️ Room Database -->
        <activity android:name="com.mobilecomponetlab.GisellaPutri.roomdb.RoomActivity" />

        <!-- ⚙️ Shared Preferences -->
        <activity android:name="com.mobilecomponetlab.GisellaPutri.sharedprefs.SharedPrefsActivity" />

    </application>
</manifest>
